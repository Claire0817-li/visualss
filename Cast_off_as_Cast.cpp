//     /ᐠ˶˶ᐟ\
//   (=•ㅅ < )
//  |￣∪∪￣|
//  |＿求AC＿|
// 猫神大人请赐我本世纪最难得的运气[^]_[^]
#include <bits/stdc++.h>
using namespace std;
using ll=long long;
using vi=vector<int>;
using vl=vector<ll>;
using vvi=vector<vi>;
using vvl=vector<vl>;
struct art
{
    int r,di1,di2;
}p[100005];
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n,rs,ds,rt,dt;
    cin>>n>>rs>>ds>>rt>>dt;
    for(int i=0;i<n;i++)
    {
        cin>>p[i].r>>p[i].di1>>p[i].di2;
    }
    return 0;
}